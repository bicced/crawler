{
  "name": "crawler",
  "version": "0.0.0",
  "description": "Jeff's web crawler",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^1.15.0"
  },
  "devDependencies": {
    "@types/puppeteer": "^1.12.4",
    "typescript": "^3.4.5"
  }
}
